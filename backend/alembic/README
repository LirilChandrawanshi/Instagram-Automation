Generic single-database configuration with async support.
Database URL is read from app.config (env: DATABASE_URL).
Run from backend directory with PYTHONPATH=. or from project root with PYTHONPATH=backend.

  alembic upgrade head
  alembic revision --autogenerate -m "description"
  alembic downgrade -1
